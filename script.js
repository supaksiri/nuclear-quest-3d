import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.170.0/build/three.module.js';
const host=document.querySelector('#scene');const scene=new THREE.Scene();scene.fog=new THREE.Fog(0x07111f,10,35);const camera=new THREE.PerspectiveCamera(55,host.clientWidth/host.clientHeight,.1,100);camera.position.set(0,4,12);const renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(host.clientWidth,host.clientHeight);host.appendChild(renderer.domElement);scene.add(new THREE.AmbientLight(0x7acfff,1.5));const light=new THREE.PointLight(0x9b6cff,40,30);light.position.set(3,6,6);scene.add(light);
const nucleus=new THREE.Group();scene.add(nucleus);for(let i=0;i<34;i++){const g=new THREE.SphereGeometry(.34,20,20);const m=new THREE.MeshStandardMaterial({color:i%2?0xff5f75:0x50d6ff,roughness:.35,metalness:.2,emissive:i%2?0x33000a:0x002a33});const p=new THREE.Mesh(g,m);const r=1.5*Math.cbrt(Math.random()),a=Math.random()*Math.PI*2,b=Math.acos(2*Math.random()-1);p.position.set(r*Math.sin(b)*Math.cos(a),r*Math.sin(b)*Math.sin(a),r*Math.cos(b));nucleus.add(p)}
const shield=new THREE.Mesh(new THREE.BoxGeometry(.28,5.4,5.4),new THREE.MeshPhysicalMaterial({color:0xb9d6e8,transparent:true,opacity:.45,roughness:.2}));shield.position.x=3.2;scene.add(shield);const rays=new THREE.Group();scene.add(rays);let rayType='alpha',shieldType='paper';
function rebuildRays(){rays.clear();const cfg={alpha:{color:0xffca58,count:20,speed:.035,penetration:{paper:0,aluminum:0,lead:0}},beta:{color:0x55f0ff,count:28,speed:.06,penetration:{paper:1,aluminum:0,lead:0}},gamma:{color:0xc785ff,count:38,speed:.09,penetration:{paper:1,aluminum:1,lead:.35}}}[rayType];for(let i=0;i<cfg.count;i++){const dot=new THREE.Mesh(new THREE.SphereGeometry(rayType==='gamma'?.07:.1,10,10),new THREE.MeshBasicMaterial({color:cfg.color}));dot.position.set(-1.5,(Math.random()-.5)*2.8,(Math.random()-.5)*2.8);dot.userData={speed:cfg.speed*(.7+Math.random()*.6),limit:cfg.penetration[shieldType]?7:3.05};rays.add(dot)}}rebuildRays();
const shieldColors={paper:0xe9e1c8,aluminum:0xb8c7d6,lead:0x596474};document.querySelectorAll('[data-rad]').forEach(b=>b.onclick=()=>{rayType=b.dataset.rad;document.querySelectorAll('[data-rad]').forEach(x=>x.classList.toggle('active',x===b));document.querySelector('#lesson-text').textContent={alpha:'แอลฟามีอำนาจก่อไอออนสูง แต่กระดาษกั้นได้',beta:'บีตาทะลุกระดาษได้ แต่อะลูมิเนียมช่วยกั้น',gamma:'แกมมาทะลุผ่านสูง ต้องใช้ตะกั่วหรือคอนกรีต'}[rayType];rebuildRays()});document.querySelectorAll('[data-shield]').forEach(b=>b.onclick=()=>{shieldType=b.dataset.shield;shield.material.color.setHex(shieldColors[shieldType]);shield.material.opacity=shieldType==='paper'?.25:shieldType==='aluminum'?.48:.72;document.querySelectorAll('[data-shield]').forEach(x=>x.classList.toggle('active',x===b));rebuildRays()});
function animate(){requestAnimationFrame(animate);nucleus.rotation.y+=.004;nucleus.rotation.x+=.0015;rays.children.forEach(d=>{d.position.x+=d.userData.speed;if(d.position.x>d.userData.limit){d.position.x=-1.5;d.position.y=(Math.random()-.5)*2.8;d.position.z=(Math.random()-.5)*2.8}});renderer.render(scene,camera)}animate();addEventListener('resize',()=>{camera.aspect=host.clientWidth/host.clientHeight;camera.updateProjectionMatrix();renderer.setSize(host.clientWidth,host.clientHeight)});
const n0=document.querySelector('#n0'),halves=document.querySelector('#halves'),hv=document.querySelector('#halves-value'),remaining=document.querySelector('#remaining'),canvas=document.querySelector('#chart'),ctx=canvas.getContext('2d');function draw(){const N=Math.max(1,+n0.value||1),h=+halves.value;hv.textContent=h;remaining.textContent=(N*Math.pow(.5,h)).toLocaleString(undefined,{maximumFractionDigits:2});ctx.clearRect(0,0,canvas.width,canvas.height);ctx.strokeStyle='#264b69';ctx.lineWidth=1;for(let i=0;i<=10;i++){const x=45+i*(canvas.width-70)/10;ctx.beginPath();ctx.moveTo(x,20);ctx.lineTo(x,canvas.height-35);ctx.stroke()}ctx.strokeStyle='#4de2ff';ctx.lineWidth=4;ctx.beginPath();for(let i=0;i<=200;i++){const x=45+i*(canvas.width-70)/200,y=20+(1-Math.pow(.5,i/20))*(canvas.height-55);i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.stroke();ctx.fillStyle='#eef7ff';ctx.font='18px Prompt';ctx.fillText('ปริมาณสารที่เหลือ',48,22);ctx.fillStyle='#9b6cff';const px=45+h*(canvas.width-70)/10,py=20+(1-Math.pow(.5,h))*(canvas.height-55);ctx.beginPath();ctx.arc(px,py,7,0,Math.PI*2);ctx.fill()}n0.oninput=draw;halves.oninput=draw;draw();